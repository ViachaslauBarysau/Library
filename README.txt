    Application uses H2 In-memory database and Jetty plugin for easy starting and reviewing. First, add new Run
Configuration, choose Maven configuration and type there into the "Command line" field - "jetty:run". Configuration
represents at running_options.png file. Now save this configuration and run application using it. Application is
available at http://localhost:8080/.